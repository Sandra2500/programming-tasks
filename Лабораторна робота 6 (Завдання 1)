# Функція для вставки елемента в список на задану позицію
def insert_element(lst, element, position):
    # Вставляємо елемент на вказану позицію
    lst.insert(position, element)

# Основна частина програми
# Користувач вводить список елементів
user_input = input("Введіть список елементів через пробіл: ")
lst = list(map(int, user_input.split()))  # Перетворюємо введені значення в список цілих чисел

# Користувач вводить елемент для вставки та позицію
element = int(input("Введіть елемент, який хочете вставити: "))
position = int(input("Введіть позицію, на яку хочете вставити елемент (нумерація з 0): "))

# Викликаємо функцію для вставки елемента
insert_element(lst, element, position)

# Виводимо оновлений список
print("Оновлений список:", lst)
