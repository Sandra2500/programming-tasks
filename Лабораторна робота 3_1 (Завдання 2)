# Користувач вводить слово
word = input("Введіть слово: ")

# Створюємо порожній список для збереження літер, які зустрічаються більше одного разу
duplicates = []

# Перебираємо кожну літеру в слові
for letter in set(word):  # Використовуємо set, щоб перевірити кожну літеру лише один раз
    if word.count(letter) > 1:  # Якщо літер зустрічається більше одного разу
        duplicates.append(letter)  # Додаємо її в список duplicates

# Виводимо знайдені літери
if duplicates:
    print("Однакові літери:", ", ".join(duplicates))
else:
    print("Однакових літер не знайдено.")
