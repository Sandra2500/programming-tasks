# Модуль із функцією для обчислення сумарного шляху

def calculate_total_distance(n):
    """Обчислює сумарний шлях пробіжок спортсмена за n днів."""
    distance = 10  # Відстань у перший день
    total_distance = 0

    for day in range(n):
        total_distance += distance
        distance *= 1.1  # Щоденне збільшення на 10%

    return total_distance
