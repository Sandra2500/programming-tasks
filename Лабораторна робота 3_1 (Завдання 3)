# Користувач вводить речення
sentence = input("Введіть речення: ")

# Розбиваємо речення на окремі слова
words = sentence.split()

# Лічильник слів, що починаються з літери 'н' (незалежно від регістру)
count = 0

# Перевіряємо кожне слово
for word in words:
    if word.lower().startswith('н'):  # Перевірка, чи починається слово з 'н'
        count += 1

# Виводимо результат
print(f"Кількість слів, що починаються з літери 'н': {count}")
